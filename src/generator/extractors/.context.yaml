version: 1
last_updated: 2026-04-14T19:50:25.964Z
fingerprint: f8ea3f83
scope: src/generator/extractors
summary: Source directory.
maintenance: "Read-only visit: don't modify this file. Changed code here? Update `summary`, `decisions`, `constraints`;
  run `context rehash` (never edit `fingerprint`). No secrets."
dependencies:
  internal:
    - ../../core/scanner.js
    - ../ast.js
    - ./shared.js
data_models:
  - "interface Candidate { filename: string | RegExp, fact: (content: string) => string | null }"
  - 'interface ExtractedModel { kind: "interface" | "type" | "class" | "datacl…, name: string, fields: Array<{ name:
    string; type: string }>, file: string, order: number }'
  - "interface Pattern { regex: RegExp, label: (match: RegExpMatchArray) => string }"
exports:
  - "async function extractConfig(scanResult: ScanResult): Promise<string[]>"
  - "async function extractDataModels(scanResult: ScanResult): Promise<string[]>"
  - "async function extractEnvironment(scanResult: ScanResult): Promise<string[]>"
  - "async function extractEvents(scanResult: ScanResult): Promise<string[]>"
  - "async function readIfExists(path: string): Promise<string | null>"
  - "async function statIfExists(path: string): Promise<boolean>"
  - "function stripComments(content: string, ext: string): string"
  - "async function extractTesting(scanResult: ScanResult): Promise<string[]>"
  - "async function extractTodos(scanResult: ScanResult): Promise<string[]>"
imports:
  - path: ../../core/scanner.js
    symbols:
      - ScanResult
  - path: ../ast.js
    symbols:
      - getGrammarsDir
      - getLanguage
      - getParser
      - getQuery
      - loadTreeSitter
  - path: ./shared.js
    symbols:
      - readIfExists
      - statIfExists
      - stripComments
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - exports
  - dependencies.internal
  - imports
  - data_models
semantic_fingerprint: c32875d13de6

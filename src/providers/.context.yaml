version: 1
last_updated: 2026-04-14T19:50:26.181Z
fingerprint: ba8d8701
scope: src/providers
summary: Abstract LLM provider interface.
maintenance: "Read-only visit: don't modify this file. Changed code here? Update `summary`, `decisions`, `constraints`;
  run `context rehash` (never edit `fingerprint`). No secrets."
dependencies:
  internal:
    - ./index.js
data_models:
  - "class AnthropicProvider { name: unknown, model: string, client: Anthropic }"
  - "class GoogleProvider { name: unknown, model: string, apiKey: string }"
  - "interface LLMProvider { name: string, model: string }"
  - "class OllamaProvider { name: unknown, model: string, baseUrl: string }"
  - "class OpenAIProvider { name: unknown, model: string, client: OpenAI }"
exports:
  - class AnthropicProvider
  - class GoogleProvider
  - interface LLMProvider
  - type ProviderName
  - "async function createProvider( name: ProviderName, credential?: string, model?: string, ): Promise<LLMProvider>"
  - class OllamaProvider
  - class OpenAIProvider
imports:
  - path: ./index.js
    symbols:
      - LLMProvider
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - exports
  - dependencies.internal
  - imports
  - data_models
semantic_fingerprint: 9e2387bdb8ba

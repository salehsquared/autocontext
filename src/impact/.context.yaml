version: 1
last_updated: 2026-04-14T19:50:26.035Z
fingerprint: e4a3af17
scope: src/impact
summary: Documentation.
maintenance: "Read-only visit: don't modify this file. Changed code here? Update `summary`, `decisions`, `constraints`;
  run `context rehash` (never edit `fingerprint`). No secrets."
dependencies:
  internal:
    - ../index/store.js
    - ../index/types.js
data_models:
  - 'interface ImpactSeed { file: FileId, symbol: string, reason: "file" | "symbol" | "diff" }'
  - 'interface ImpactEdge { from: FileId, to: FileId, kind: "reference" | "import" }'
  - "interface ImpactAffected { file: FileId, hops: number, via: ImpactEdge[], context_scope: string }"
  - "interface ImpactAffectedScope { scope: string, min_hops: number, file_count: number }"
  - "interface ImpactReport { seeds: Array, affected: ImpactAffected[], affected_scopes: ImpactAffectedScope[], stopped:
    Array, caveat: string }"
  - "interface ImpactOptions { maxDepth: number, maxResults: number }"
exports:
  - interface ImpactSeed
  - interface ImpactEdge
  - interface ImpactAffected
  - interface ImpactAffectedScope
  - interface ImpactReport
  - IMPACT_CAVEAT
  - interface ImpactOptions
  - "async function computeImpact( store: IndexStore, seeds: ImpactSeed[], opts: ImpactOptions ="
imports:
  - path: ../index/store.js
    symbols:
      - IndexStore
  - path: ../index/types.js
    symbols:
      - FileId
      - IndexSymbol
derived_fields:
  - version
  - last_updated
  - fingerprint
  - scope
  - exports
  - dependencies.internal
  - imports
  - data_models
semantic_fingerprint: d1ed28329d10
